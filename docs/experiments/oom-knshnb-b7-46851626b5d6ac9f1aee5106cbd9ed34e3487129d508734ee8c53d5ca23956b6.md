# oom: knshnb-b7

Recorded at: 2026-05-18T17:59:29

B7 OOM at original plan config (batch_size=4, image_size=1024x1024, tf_efficientnet_b7_ns, fp16 AMP). Run folder: results/2026-05-17-170751-knshnb-b7. Wrapper exited with subprocess.CalledProcessError after the child training process crashed.

Failure point: torch.OutOfMemoryError. PyTorch tried to allocate 30.00 MiB more; only 4.12 MiB was free on GPU 0 (total 15.46 GiB; PyTorch allocated 14.59 GiB; non-PyTorch process 260 MiB). Tighter than the B6 OOM at the same image_size (B6 had 42 MiB free).

Hardware: 2x NVIDIA GeForce RTX 5080 16.6 GB each, DDP across both. Same hardware that just successfully trained B6 at batch_size=3, accumulate_grad_batches=2 on a ~24h 50min run.

Next retry per plan Task 10 Step 7: batch_size=2, accumulate_grad_batches=2 (effective batch=4, same as original). Copying config to <RUN_DIR>/configs/efficientnet_b7_batch2_accum2.yaml.
